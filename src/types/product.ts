export interface NewProduct {
  id: number;
  name: string;
  description: string;
  price: number;
}

export interface DataProduct {
  id: number;
  name: string;
  description: string;
  price: number;
}
