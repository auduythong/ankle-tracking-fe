export interface NewAirport {
  id: number;
  name: string;
  code: string;
  latLocation: string;
  longLocation: string;
}

export interface DataAirport {
  id: number;
  name: string;
  code: string;
  lat_location: string;
  long_location: string;
}
